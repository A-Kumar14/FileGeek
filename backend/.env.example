# ──────────────────────────────────────────────────────────────────────────────
# FileGeek Backend — Environment Variables Reference
# Copy this file to .env and fill in your values.
# DO NOT commit .env to git (it is in .gitignore).
# ──────────────────────────────────────────────────────────────────────────────

# ── REQUIRED ──────────────────────────────────────────────────────────────────

# JWT signing secret — server WILL NOT START without this.
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=

# ── REQUIRED (at least one) ───────────────────────────────────────────────────

# OpenRouter — preferred provider (access to GPT-4o, Gemini, etc. via one key)
OPENROUTER_API_KEY=

# OpenAI — also required if you use Whisper (transcription) or TTS
OPENAI_API_KEY=

# Google Gemini
GOOGLE_API_KEY=

# ── STRONGLY RECOMMENDED FOR PRODUCTION ───────────────────────────────────────

# Redis — required for Celery async document indexing and Socket.IO progress
REDIS_URL=redis://localhost:6379/0

# Set to "true" in production so httpOnly refresh cookies require HTTPS
HTTPS_ONLY=true

# ── OPTIONAL — AI MODEL OVERRIDES ─────────────────────────────────────────────

# Override the default OpenRouter chat model (default: openai/gpt-4o)
# OPENROUTER_CHAT_MODEL=openai/gpt-4o

# Number of RAG chunks to retrieve per query (default: 5)
# NUM_RETRIEVAL_CHUNKS=5

# Chunks to retrieve in deep-think mode (default: 12)
# DEEP_THINK_CHUNKS=12

# ── OPTIONAL — STORAGE ────────────────────────────────────────────────────────

# SQLite async database URL (default: sqlite+aiosqlite:///./instance/users.db)
# DATABASE_URL=sqlite+aiosqlite:///./instance/users.db

# SQLite sync URL for Celery workers (default: sqlite:///./instance/users.db)
# SYNC_DATABASE_URL=sqlite:///./instance/users.db

# File upload directory (default: uploads)
# UPLOAD_FOLDER=uploads

# ── OPTIONAL — AWS S3 (for cloud file storage) ────────────────────────────────

# S3_ENABLED=false
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_S3_REGION=us-east-1

# ── OPTIONAL — CORS ───────────────────────────────────────────────────────────

# Additional allowed origins beyond the defaults (comma-separated)
# CORS_ORIGINS=https://your-custom-domain.com

# ── OPTIONAL — SERVER ─────────────────────────────────────────────────────────

# FLASK_PORT=5001
# LOG_LEVEL=INFO
