// UploadThing stubbed out — file upload handled directly via local File objects.
// Stub exports prevent crashes if any component still imports from this module.
export const uploadFiles = async () => [];
export const useUploadThing = () => ({ startUpload: async () => [], isUploading: false });
